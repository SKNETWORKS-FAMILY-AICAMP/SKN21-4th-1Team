# 🎯 법령 검색 Q&A 챗봇 - 발표 가이드

> **목표**: 비전공자도 이해할 수 있는 프로젝트 설명

---

## 📌 **프로젝트 한 문장 설명**

**"노동법을 모르는 일반인들도 쉽게 물어볼 수 있는, 정확한 답변을 주는 AI 챗봇"**

- 사용자가 질문 입력 → AI가 법령 데이터에서 찾아서 답변 제공

---

## 🎬 **프로젝트 흐름도 (간단한 버전)**

```
사용자 입력
    ↓
브라우저에서 질문 전송
    ↓
Django 웹 서버에서 받음
    ↓
RAG 시스템 (AI)
  ├─ 1️⃣ 법령 데이터에서 관련 내용 검색
  ├─ 2️⃣ 검색 결과 정렬
  └─ 3️⃣ GPT-4o로 자연스럽게 답변 생성
    ↓
사용자에게 답변 표시
```

---

## 📁 **프로젝트 파일 구조 (최소 설명)**

```
SKN21-4th-1Team/
│
├── 📄 manage.py              ← Django 제어 센터 (서버 시작)
│
├── 🔧 config/                ← 전체 프로젝트 설정
│   ├── settings.py           ← 데이터베이스, 앱 등록, 세션 설정
│   └── urls.py               ← 어느 URL이 어느 파일로 갈지 결정
│
├── 🏠 home/                  ← 홈페이지
│   ├── views.py              ← 홈페이지 표시 로직
│   └── urls.py               ← "/" 접속하면 여기로
│
├── 💬 chat/                  ← 핵심! 챗봇 앱
│   ├── views.py              ← 채팅 화면 & 챗봇 로직
│   ├── models.py             ← 대화 기록 저장 구조
│   ├── services.py           ← RAG 시스템과 통신
│   ├── ai_module/
│   │   └── chatbot_graph_V8_FINAL.py  ← RAG 핵심 (검색→정렬→답변)
│   └── urls.py               ← "/chat/" 접속하면 여기로
│
├── 👤 accounts/              ← 로그인/회원가입
│   ├── views.py              ← 로그아웃 로직
│   ├── urls.py               ← 로그인 관련 URL
│   └── templates/account/    ← 로그인 HTML
│
├── 📋 templates/             ← HTML 파일들 (화면 디자인)
│   ├── base.html             ← 모든 페이지 공통 틀
│   ├── account/login.html    ← 로그인 화면
│   ├── chat/chat.html        ← 채팅 화면
│   └── home/home.html        ← 홈 화면
│
├── 🗄️ db.sqlite3             ← 데이터베이스 (사용자, 대화 저장)
│
└── .env                      ← 비밀 설정 (OpenAI API 키 등)
```

---

## 🔄 **사용자 방문부터 답변까지 (상세 흐름)**

### **1️⃣ 사용자 홈페이지 방문**

```
브라우저에서 "http://localhost:8000/" 입력
        ↓
home/urls.py에서 경로 확인
        ↓
home/views.py의 home() 함수 실행
        ↓
templates/home/home.html 화면 표시
```

**파일**: `home/views.py`
```python
def home(request):
    return render(request, 'home/home.html')  # 홈 화면 표시
```

---

### **2️⃣ 사용자 로그인**

```
사용자가 "로그인" 버튼 클릭
        ↓
/accounts/login/ (또는 /auth/login/) 페이지로 이동
        ↓
templates/account/login.html 화면 표시
        ↓
사용자가 이메일/비밀번호 입력 후 로그인 버튼 클릭
        ↓
Django의 allauth 라이브러리가 사용자 인증
        ↓
✅ 세션(Session) 생성 = "이 사용자는 로그인했다"고 기억
        ↓
/chat/ 페이지로 리다이렉트 (이동)
```

**설정**: `config/settings.py`
```python
# allauth 라이브러리가 자동 처리
SOCIALACCOUNT_LOGIN_ON_GET = True  # Google 로그인 지원
LOGIN_REDIRECT_URL = "/chat/"  # 로그인 후 채팅 페이지로
```

---

### **3️⃣ 채팅 페이지 진입**

```
브라우저: "http://localhost:8000/chat/" 접속
        ↓
chat/urls.py에서 경로 확인
        ↓
chat/views.py의 chat() 함수 실행
        ↓
templates/chat/chat.html 채팅 화면 표시
        ↓
화면에 텍스트 입력창과 전송 버튼이 보임
```

**파일**: `chat/views.py`
```python
def chat(request):
    return render(request, 'chat/chat.html', {
        "chat_history": request.session.get("chat_history", [])
    })
```

---

### **4️⃣ 사용자가 질문 입력 및 전송**

```
사용자가 텍스트 입력 (예: "퇴직금을 못 받으면 어떻게 하죠?")
        ↓
templates/chat/chat.html의 JavaScript에서 감지
        ↓
fetch() 함수로 /chat/api/ 경로에 POST 요청 (HTTP 통신)
        ↓
chat/views.py의 chat_api() 함수 실행
```

**파일**: `templates/chat/chat.html` (JavaScript 부분)
```javascript
const messageInput = document.querySelector('#message-input');
messageInput.addEventListener('keyup', function(e) {
    if (e.key === 'Enter') {
        const message = this.value;
        
        // 서버로 메시지 전송 (POST 요청)
        fetch('/chat/api/', {
            method: 'POST',
            headers: {'Content-Type': 'application/json'},
            body: JSON.stringify({message: message})
        })
        .then(response => response.json())
        .then(data => {
            // 답변 받음
            console.log(data.answer);
        });
    }
});
```

---

### **5️⃣ Django 서버에서 처리 (핵심!)**

```
chat/views.py의 chat_api() 함수 실행
        ↓
ChatbotService.get_instance() 호출
        ↓
/////////////////////////////////////////////////////////////
/// 🤖 RAG 시스템 시작! (AI가 답변 생성하는 부분)
/////////////////////////////////////////////////////////////
        ↓
chat/services.py에서 ChatbotService 초기화
        ↓
chat/ai_module/chatbot_graph_V8_FINAL.py 실행
        │
        ├─ 단계 1️⃣: 질문 이해
        │  "퇴직금을 못 받으면?" → 무엇을 묻는지 분석
        │
        ├─ 단계 2️⃣: 법령 데이터 검색
        │  Qdrant 벡터 DB (법령 저장소)에서 검색
        │  비슷한 조문들을 찾음 (예: 근로기준법 34조)
        │
        ├─ 단계 3️⃣: 검색 결과 정렬
        │  여러 조문 중에서 가장 관련성 높은 것만 선택
        │  (Jina Reranker 사용)
        │
        └─ 단계 4️⃣: 답변 생성
           OpenAI의 GPT-4o에 지시문 전달:
           "이 법령 조문을 보고 사용자의 질문에 답해줘"
           → GPT-4o가 자연스러운 답변 생성
        ↓
답변: "근로기준법 34조에 따르면, 회사가 
      퇴직금을 지급하지 않으면 고용노동부에 
      신고할 수 있습니다..."
        ↓
```

**파일**: `chat/services.py`
```python
async def get_response(self, user_message: str):
    """사용자 질문에 답변 생성"""
    
    # RAG 그래프 준비
    if not self._graph:
        await self._async_graph_build()
    
    # 질문과 함께 그래프 실행
    inputs = {
        "user_query": user_message,
        "messages": [("user", user_message)],
        "retry_count": 0
    }
    
    result = await self._graph.ainvoke(inputs)
    return result.get("generated_answer", "죄송합니다.")
```

**파일**: `chat/ai_module/chatbot_graph_V8_FINAL.py` (RAG 핵심)
```python
# 1️⃣ 벡터 저장소에서 법령 검색
retriever = VectorStoreManager(config)

# 2️⃣ 검색 결과 정렬
reranker = JinaReranker(model_name="jina-reranker-v1-base-en")

# 3️⃣ GPT-4o로 답변 생성
llm = ChatOpenAI(model_name="gpt-4o-mini")

# 4️⃣ 이들을 LangGraph로 연결
graph = StateGraph(AgentState)
graph.add_node("retrieve", retrieve_documents)
graph.add_node("rerank", rerank_documents)
graph.add_node("generate", generate_answer)
# ... 연결 ...
```

---

### **6️⃣ 사용자에게 답변 표시**

```
JavaScript에서 답변 받음
        ↓
화면의 채팅창에 답변 표시
        ↓
chat/models.py의 ChatMessage 모델로 DB에 저장
        ↓
"질문: 퇴직금을 못 받으면?"  ← 사용자 메시지
"답변: 근로기준법 34조..."   ← AI 답변
        ↓
다음 질문 대기
```

---

## 🔐 **로그인/세션 시스템**

### **로그인 흐름**

```
사용자 입력
  ↓
django-allauth (외부 라이브러리)
  ├─ 이메일/비밀번호 검증
  ├─ 데이터베이스에서 사용자 확인
  ├─ SessionMiddleware가 세션 생성
  └─ 쿠키에 세션 ID 저장
  ↓
✅ 로그인 완료
```

**파일**: `config/settings.py`
```python
MIDDLEWARE = [
    # ...
    "django.contrib.sessions.middleware.SessionMiddleware",  # ← 세션 관리
    "django.contrib.auth.middleware.AuthenticationMiddleware",  # ← 인증 처리
    "allauth.account.middleware.AccountMiddleware",  # ← allauth 처리
]

# 세션 설정
SESSION_ENGINE = "django.contrib.sessions.backends.cache"  # 메모리에 저장
SESSION_EXPIRE_AT_BROWSER_CLOSE = True  # 브라우저 닫으면 로그아웃
```

### **로그아웃 흐름**

```
사용자가 로그아웃 버튼 클릭
  ↓
accounts/urls.py의 logout 경로로 이동
  ↓
accounts/views.py의 logout_view() 함수 실행
  ↓
Django의 logout() 함수 호출
  ├─ 데이터베이스의 세션 삭제
  ├─ 쿠키 삭제
  └─ 사용자 인증 정보 삭제
  ↓
✅ 로그아웃 완료 → 홈 페이지로 리다이렉트
```

**파일**: `accounts/views.py`
```python
def logout_view(request):
    logout(request)  # Django 기본 로그아웃
    return redirect('/')  # 홈으로 이동
```

---

## 📊 **데이터베이스 (DB.sqlite3) - 뭐가 저장되나?**

```
db.sqlite3 (SQLite 데이터베이스)
│
├─ auth_user
│  ├─ id: 1
│  ├─ email: user@example.com
│  ├─ password: (해시된 비밀번호)
│  └─ ...
│
├─ sessions_session
│  ├─ session_key: "abc123xyz..."
│  ├─ session_data: {user_id: 1, ...}
│  └─ expire_date: 2026-01-30 10:00
│
├─ chat_chatmessage
│  ├─ id: 1
│  ├─ user: user@example.com
│  ├─ role: "user"
│  ├─ message: "퇴직금을 못 받으면?"
│  ├─ created_at: 2026-01-29 10:30
│  └─ ...
│
└─ django_migrations
   ├─ app: "chat"
   ├─ name: "0001_initial"
   └─ ...
```

---

## 🚀 **실제 실행 순서 (커맨드 라인)**

### **1단계: 필수 패키지 설치**
```bash
pip install -r requirements.txt
```
- Django, LangChain, OpenAI, Qdrant 등 모든 필요한 라이브러리 설치

### **2단계: 데이터베이스 초기화**
```bash
python manage.py migrate
```
- 테이블 생성 (auth_user, sessions_session, chat_chatmessage 등)

### **3단계: 관리자 계정 생성 (선택)**
```bash
python manage.py createsuperuser
```
- 이메일/비밀번호 입력 후 Django Admin 접근 가능

### **4단계: 서버 시작**
```bash
python manage.py runserver
```
- `http://localhost:8000/` 에서 서비스 접근

### **5단계: 브라우저에서 접속**
```
http://localhost:8000/  → 홈페이지
http://localhost:8000/accounts/login/  → 로그인
http://localhost:8000/chat/  → 채팅 (로그인 필요)
```

---

## 🛠️ **사용된 주요 라이브러리**

| 라이브러리 | 역할 | 예시 |
|----------|------|------|
| **Django** | 웹 서버 | URL 라우팅, 데이터베이스 관리 |
| **allauth** | 로그인/회원가입 | Google 소셜 로그인, 이메일 인증 |
| **LangChain** | RAG 시스템 | 벡터 검색, 프롬프트 관리 |
| **Qdrant** | 벡터 데이터베이스 | 법령 데이터 저장 및 검색 |
| **OpenAI** | LLM | GPT-4o 모델 활용 |
| **SQLite** | 일반 데이터베이스 | 사용자, 세션, 대화 저장 |

---

## 📝 **요약 (한눈에 보기)**

```
┌─────────────────────────────────────────────────────────────┐
│                   프로젝트 동작 원리                         │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  사용자 입력                                                │
│       ↓                                                      │
│  Django (Web Server)                                        │
│  ├─ URL 라우팅                                              │
│  ├─ 세션/인증 관리                                          │
│  └─ views.py 실행                                           │
│       ↓                                                      │
│  RAG 시스템 (AI)                                            │
│  ├─ LangChain 처리                                          │
│  ├─ Qdrant 벡터 검색                                        │
│  └─ OpenAI GPT-4o 답변 생성                                │
│       ↓                                                      │
│  SQLite DB에 저장                                           │
│       ↓                                                      │
│  화면에 답변 표시                                           │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 💡 **비전공자를 위한 비유**

### **Django = 식당의 웨이터**
- 고객(사용자)의 주문 받음
- 주방(RAG 시스템)에 전달
- 음식 받아서 고객에게 제공

### **RAG 시스템 = 도서관 사서**
- 고객이 "법령 정보 원해?"라고 하면
- 도서관(Qdrant)에서 관련 책 찾음
- 최고 관리사(Reranker)가 최고 책 선택
- 인공지능(GPT-4o)이 설명해줌

### **세션 = 손님 기억력**
- "이 손님은 VIP입니다" 라고 메모
- 나중에 와도 기억함
- 브라우저 닫으면 메모 삭제 (로그아웃)

---

## 🎯 **발표 시 강조할 부분**

✅ **실제 법령 데이터**를 사용해서 정확한 답변 제공
✅ **누구나 쉽게 사용**할 수 있는 웹 인터페이스
✅ **AI 기술 (RAG + LLM)**의 실제 활용 사례
✅ **로그인/세션 관리**로 개인 정보 보호
✅ **확장 가능한 구조** (형사법, 민사법 등 추가 가능)

---

생성: 2026-01-29
